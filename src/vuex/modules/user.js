import * as types from '../mutation-types'

// initial state
const state = {
  userInfo: {}
}

console.log(types)

// mutations
const mutations = {
  // [types.SET_USERINFO] (state, userInfo) {
  //   this.userInfo = userInfo
  // }
}

export default {
  state,
  mutations
}
